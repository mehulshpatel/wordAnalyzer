package com.rest.wordAnalyzer.controller;

public class WordAnalyzerControllerTest {
	
	/*
	 * @Autowired private WordAnalyzerController wordAnalyzerController;
	 * 
	 * @Autowired private MockMvc mockMvc;
	 * 
	 * @MockBean private WordFrequencyAnalyzer wordFrequencyAnalyzer;
	 * 
	 * @Autowired WebApplicationContext webApplicationContext;
	 * 
	 * private static final String TEXT_PARAM_NAME = "text";
	 * 
	 * private static final String TEST_TEXT = "the hat the happy the hat why";
	 * 
	 * protected void setUp() { mockMvc =
	 * MockMvcBuilders.webAppContextSetup(webApplicationContext).build(); }
	 * 
	 * @Test public void getHigestfrequcyForGivenText() throws Exception {
	 * 
	 * this.mockMvc.perform(get("/wordAnalyzer/highest").param(TEXT_PARAM_NAME,
	 * TEST_TEXT)).andDo(print()).andExpect(status().isOk()); }
	 */
}
